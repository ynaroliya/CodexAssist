# Local-only seed configuration. Set non-production values in .env.local; never commit that file.
DPM_DEMO_PASSWORD=replace-with-a-local-demo-password
# Optional: omit to generate an ephemeral development-only session signing key on startup.
# DPM_SESSION_SECRET=replace-with-a-long-random-local-secret
